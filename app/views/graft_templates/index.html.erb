{{ users.greeting }}
{{ users.sayHello() }}

<div id="search">
	<input ng-model="query" placeholder="find politician" ng-keyup="$event.keyCode == 13 && executeSearch()">
	<div class="repo"  ng-repeat="repo in repos" ng-click="openRepo(repo.full_name)">
		<strong> {{repo.full_name}}</strong>
		<p>{{repo.description}}</p>
	</div>
</div>

{{ users.greeting }}
{{ users.sayHello() }}