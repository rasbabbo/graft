{{grafts.sayHello()}}

{{grafts.candidatesInfo()}}

<form ng-submit="grafts.findBy(id)">
	<input type="text" ng-model="id">
	<button> Submit </button>
</form>


<form ng-submit="grafts.findPers(cid)">
	<ng-repeat="pol in pols">
		<button ng-click="grafts.findPers(cid)">
		{{attributes.firstlast}}{{attributes.cid}}
		</button>
</form>

<!-- <div ng-repeat="pol in pols.candidatesInfo">
	<button ng-click="grafts.findPers(cid)">{{@attributes.firstlast}}{{@attributes.cid}}</button>
</div> -->


